const Sum = (A, B, C) => 
{
 let Submarks;
 Submarks = (A + B +C);
 return Submarks;
};

const Average = (A, B, C) => 
{
 let marks;
 marks =((A+B+C)/3);
 return marks;
};
console.log(Sum(50,20,100));
console.log(Average(50,20,100));